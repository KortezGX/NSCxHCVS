info:
  name: 1. 使用者登入
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
       "username": "admin",
       "password": "adminpass"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
